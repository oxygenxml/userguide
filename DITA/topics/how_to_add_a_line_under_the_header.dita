<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="how_to_add_a_line_under_the_header_0">
    <title>How to Add a Line Under the Header</title>
    <prolog>
        <metadata>
            <keywords>
                <indexterm product="author developer editor authorEclipse developerEclipse editorEclipse"
                    ><ph keyref="wysywig-or-html5-transformation"/>
                    <indexterm>Header<indexterm>Underlined header</indexterm></indexterm></indexterm>
                <indexterm product="pdf-css">Header<indexterm>Underlined header</indexterm></indexterm>
            </keywords>
        </metadata>
    </prolog>
    <body>
        <p>There are two ways to add a horizontal line under the header.</p>
        <p>
            <ol id="ol_uvy_t2w_cgb">
                <li>Adding a border in the page margin-boxes<p>To add a horizontal line that would
                        strech across the width of the page, each of the 5  margin boxes in the top
                        side of the page (<codeph>top-left-corner</codeph>,
                            <codeph>top-left</codeph>, <codeph>top-center</codeph>,
                            <codeph>top-right</codeph>, <codeph>top-right-corner</codeph>) should
                        contain a bottom border.</p>Further, if you consider that the space between
                    the header and the bottom border is to large, you could also change the
                    alignment, by adding a "<codeph>vertical-align: bottom;</codeph>" declaration in
                    the page margin boxes.<p>For example, if you need to set some text as a header
                        in the top-left margin box and insert a horizontal line under it, in the
                        customization CSS you should use something
                        like:<codeblock>@page chapter, chapter:first:left:right, front-page{

   padding-top: 1em;
 
  @top-left {
    content: "Custom header";
    color: gray;
    border-bottom: 1px solid black;
    vertical-align: bottom;
  }

  @top-center{
    content:" ";
    border-bottom: 1px solid black;
    vertical-align: bottom;
  }

  @top-right{
    content:" ";
    border-bottom: 1px solid black;
    vertical-align: bottom;
  }

  @top-right-corner{
    content:" ";
    border-bottom: 1px solid black;
    vertical-align: bottom;
  }

  @top-left-corner{
    content:" ";
    border-bottom: 1px solid black;
    vertical-align: bottom;
  }</codeblock></p>The
                        "<codeph>padding-top: 1em;</codeph>" is used to avoid the border at the
                    bottom of the header joining with the page content.<p/></li>
                <li>Using a background image<p>An alterantive way to add a horizontal line/border
                        under an existing header (or in any other part of the page), consists in
                        using an SVG image artwork, like described in <xref
                            href="dcpp_how_to_add_an_image_to_the_header.dita"/>.</p></li>
            </ol>
        </p>
    </body>
</topic>
