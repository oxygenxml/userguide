<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="dcpp_how_to_avoid_page_breaks_in_lists_and_tables">
    <title>How to Avoid Page Breaks in Lists and Tables</title>
  <prolog>
    <metadata>
      <keywords>
        <indexterm>Page breaks <indexterm>Avoid in lists and tables</indexterm></indexterm>
      </keywords>
    </metadata>
  </prolog>
    <body>
        <p>For the elements that you do not want split over two pages, use the CSS property
        <codeph>page-break-inside</codeph>. If we want to impose this on tables and lists, then add
      the following to your <xref href="dcpp_the_customization_css.dita">customization
      css</xref>:</p>
        <codeblock outputclass="language-css">*[class ~= "topic/table"] {
    page-break-inside:avoid;
}
*[class ~= "topic/ol"] {
    page-break-inside:avoid;
}
*[class ~= "topic/ul"] {
    page-break-inside:avoid;
}</codeblock>
        <note>Since the task steps are inherited from the <codeph>topic/ol</codeph>, then the tasks
      also will not be broken on two separate pages. If you want to allow this, add the CSS
      rule:<codeblock outputclass="language-css">*[class ~= "task/steps"] {
    page-break-inside:auto;
}</codeblock></note>
    <note>Another way is to mark the element with an <xmlatt>outputclass</xmlatt> with the
        <codeph>page-break-avoid</codeph> value.</note>
    </body>
</topic>
