<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
  PUBLIC "-//OASIS//DTD DITA Topic//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/topic.dtd">
<topic id="xslt-stylesheet-parameters">
  <title>XSLT Stylesheet Parameters</title>
  <!--Proofed-->
  <prolog>
    <metadata>
      <keywords>
        <indexterm>Transformation Scenario<indexterm>new transformation scenario<indexterm>XSLT
              parameters</indexterm></indexterm></indexterm>
      </keywords>
    </metadata>
  </prolog>
  <body>
    <p>The global parameters of the XSLT stylesheet used in a transformation scenario can be
      configured by using the <uicontrol>Parameters</uicontrol> button in the
        <uicontrol>XSLT</uicontrol> tab of a new or edited transformation scenario dialog.</p>
    <p>The table displays all the parameters of the current XSLT stylesheet, all imported and
      included stylesheets, and all <xref
        href="additional-xslt-stylesheets.dita#additional-xslt-stylesheets" format="dita">additional
        stylesheets</xref>, along with their current values. Use the <uicontrol>Filter</uicontrol>
      text box to search for a specific term in the entire parameters collection. Note that edited
      parameters are displayed with the name in bold. </p>
    <p>If the <uicontrol>XPath</uicontrol> column is checked, the parameter value is evaluated as an
      XPath expression before starting the XSLT transformation. </p>
    <example>
      <p>For example, you can use expressions such
        as:<codeblock>doc('test.xml')//entry
//person[@atr='val']</codeblock></p>
    </example>
    <note>
      <ol>
        <li>The <b>doc</b> function solves the argument relative to the XSL stylesheet location. You
          can use full paths or editor variables (such as <varname>${cfdu}</varname> [current file
          directory]) to specify other locations: <codeph>doc('${cfdu}/test.xml')//*</codeph></li>
        <li>You cannot use XSLT Functions. Only XPath functions are allowed. </li>
      </ol>
    </note>
    <p>The following actions are available for managing parameters:</p>
    <dl>
      <dlentry>
        <dt><uicontrol>New</uicontrol></dt>
        <dd>Opens the <uicontrol>Add Parameter</uicontrol> dialog that allows you to add a new
          parameter to the list. An <xref href="editor-variables.dita#editor-variables"
            format="dita">editor variable</xref> can be inserted in the text box using the small
            (<image href="../img/InsertEditorVariables12.gif" id="image_yy5_1lz_3d"/>) button on the
          right side of the text box. If the <uicontrol>Evaluate as XPath</uicontrol> option is
          enabled, the parameter will be evaluated as an XPath expression.</dd>
      </dlentry>
      <dlentry>
        <dt><uicontrol>Edit</uicontrol></dt>
        <dd>Opens the <uicontrol>Edit Parameter</uicontrol> dialog that allows you to edit the
          selected parameter. An <xref href="editor-variables.dita#editor-variables" format="dita"
            >editor variable</xref> can be inserted in the text box using the small (<image
            href="../img/InsertEditorVariables12.gif" id="image_chq_zq2_1r"/>) button on the right
          side of the text box. If the <uicontrol>Evaluate as XPath</uicontrol> option is enabled,
          the parameter will be evaluated as an XPath expression.</dd>
      </dlentry>
      <dlentry>
        <dt><uicontrol>Unset</uicontrol></dt>
        <dd>Resets the selected parameter to its default value. Available only for parameters with
          set values.</dd>
      </dlentry>
      <dlentry>
        <dt><uicontrol>Delete</uicontrol></dt>
        <dd>Removes the selected parameter from the list. It is enabled only for parameters added to
          the list with the <uicontrol>New</uicontrol> button.</dd>
      </dlentry>
    </dl>
    <p> The bottom panel presents the following:<ul id="ul_1rt_dbb_hf">
        <li>The default value of the parameter selected in the table.</li>
        <li>A description of the parameter, if available.</li>
        <li>The system ID of the stylesheet that declares it.</li>
      </ul></p>
  </body>
  <related-links>
    <link href="editor-variables.dita#editor-variables"/>
  </related-links>
</topic>
