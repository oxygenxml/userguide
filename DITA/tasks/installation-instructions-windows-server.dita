<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task
  PUBLIC "-//OASIS//DTD DITA Task//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/task.dtd">
<task id="installation-instructions-windows-server">
  <title>Configurating Windows NT Terminal Server </title>
  <shortdesc>Windows NT Terminal Server configuration procedure. </shortdesc>
  <prolog>
    <metadata>
      <keywords>
        <indexterm>Installation <indexterm>Windows NT terminal server</indexterm></indexterm>
      </keywords>
    </metadata>
  </prolog>
  <taskbody>
    <steps>
      <step>
        <cmd><draft-comment author="Mark">The question here is, configuring it for what purpose. Why
            does it need to be configured in this way. What does it let you
          do?</draft-comment>Install <ph keyref="product"/> on the server and make its shortcuts
          available to all users.<draft-comment author="Mark">How do you make its shortcuts
            available to everyone.</draft-comment></cmd>
      </step>
      <step>
        <cmd>If you need to run multiple instances of <ph keyref="product"/>, make sure you add the
            <varname>-Dcom.oxygenxml.MultipleInstances=true</varname> parameter in the
            <filepath>.bat</filepath> startup script.</cmd>
      </step>
      <step>
        <cmd>Make sure you allocate sufficient memory to <ph keyref="product"/> by adding the
            <codeph>-Xmx</codeph> parameter either in the <filepath>.bat</filepath> or
            <filepath>.sh</filepath> startup script, or in the <filepath>.vmoptions</filepath>
          configuration file (if you start it from an executable launcher).<draft-comment
            author="Mark">How do you know if you have sufficient memory? Should you do this right
            away or wait and see if you run out of memory? What values should you use for increased
            memory?</draft-comment></cmd>
      </step>
    </steps>
  </taskbody>

</task>
