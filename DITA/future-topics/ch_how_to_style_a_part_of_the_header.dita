<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="ch_how_to_style_a_part_of_the_header">
  <title><?oxy_comment_start author="Steven" timestamp="20180919T095048+0300" comment="This topic is being moved to &quot;future-topics&quot; until we publish all manuals this fall. Then we can move it back to the dcpp map and the chemistry map.

In dcpp, it goes in &quot;Page Headers and Footers&quot; section, after the &quot;How to Decorate the Header by Using a Background Image on the Entire Page&quot;

In chemistry, it goes in the &quot;Headers and Footers&quot; section, as the last child."?>How
    to Style a Part of the Text from the Header<?oxy_comment_end?></title>
  <prolog>
    <metadata>
      <keywords>
        <indexterm product="author developer editor authorEclipse developerEclipse editorEclipse"
            ><ph keyref="wysywig-or-html5-transformation"/>
          <indexterm>Header<indexterm>Add background image</indexterm></indexterm></indexterm>
        <indexterm product="chemistry pdf-css">Header<indexterm>Style Text</indexterm></indexterm>
      </keywords>
    </metadata>
  </prolog>
  <body>
    <p>If you need to style a fragment of text (for example, a company slogan) with certain colors
      or font styles, you have several options:<ul id="ol_pn1_qcr_d2b">
        <li>Use an SVG image as background for a page margin box or for the entire page. <ph
            product="author developer editor authorEclipse developerEclipse editorEclipse pdf-css"
            >See: <xref href="dcpp_how_to_add_an_image_to_the_header.dita"/>. </ph></li>
        <li>Use the <xref
            href="https://www.oxygenxml.com/doc/ug-editor/topics/dg-oxy-label-function.html"
            format="html" scope="external"><codeph>oxy_label</codeph></xref> constructor. This is a
          function that creates a text label with a set of
          styles.<codeblock outputclass="language-css">@page {
    @top-right {
        content: oxy_label(text, "My Company", styles, "color:red; font-size: larger;") 
                 ' ' 
                 oxy_label(text, "Product", styles, "color:blue; text-decoration:underline;"));
    }
}</codeblock>You
          can combine the <codeph>oxy_label</codeph> with <codeph>oxy_xpath</codeph>, to extract and
          style a piece of text from the
            document:<codeblock outputclass="language-css">content: oxy_label(text, oxy_xpath("/some/xpath"), styles, "color:blue; "));</codeblock><note>These
            functions work only with the Chemistry CSS processor. </note></li>
        <li>Use two adjacent page margin boxes, and style them
          differently:<codeblock outputclass="language-css">@page {
  @top-center {
    content: "First part";
    color: red;
    text-align:right;
  }
  @top-left {
    content: "- Second part";
    color: blue;
    text-align:left;
  }
}</codeblock></li>
      </ul></p>
  </body>
</topic>
